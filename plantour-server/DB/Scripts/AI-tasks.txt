Нужно добавить в plantour-server систему управления юзерами. Для хравнения юзеров используется таблица PostgreSQL users. Доступ к базе чере EF, смотри PlantourContext.cs. Модель юзера в Models/User.cs. 

Создай новый DI сервис UsersService. Добавь туда необходимый код для управления юзерами. Используй подход DB-first, никаких миграций. Если нужно создать новую DB таблицу - создай SQL скрипт. Пароли должны храниться в хешированном виде. Добавь в проект всё необходимое для генерации, валидации, обновления и всех необходимых действий с JWT токенами. Добавь в UsersService методы SignUp и SignIn. Добавь AuthController с соответствующими endpoints. Все контроллеры по умолчанию должны быть [Authorize]. Добавь код и конфигурацию для подсоединения к локальной БД PostgreSQL, возьми ConnectionString из DB/Scripts/commands.txt. Предусмотри возможность задать в конфигурации свойства выпускаемого токена, например, срок действия. 
 Добавь в отдельный файл инструкцию для программиста. Используй английский язык для комментариев и инструкций.


 В Plantour есть два типа доступа: Админы и Участники. Админы могут создавать путешествия и приглашать в них Участников. Участники не могут создавать путешествия, они только могут участвовать в путешествиях, созданных Админом. 
 
 Один и тот же юзер (запись в таблице users) в зависимости от способа логина может выступать в роли Админа или в роли Участника. Если юзер логинится как Админ он не связан ни с каким Участником. Если юзер логинится как Участник он получает доступ только в пределах доступа своего Админа. Админ имеет доступ ко всем своим путешествиям. Участник имеет доступ только к тем путешествиям своего Админа, в которых он участвует. Участник не может иметь своих путешествий. 
 
 Поле users.password_hash может иметь значение или быть пустым. Если это поле имеет значение то юзер может залогиниться не только как Админ через UsersService.SignInAsync, но и как Участник через метод UsersService.SignInParticipantAsync (пока не существует). Если оно пустое, то юзер может залогинится только как Участник через метод UsersService.SignInParticipantAsynс используя admins_participants.access_code. 

 В дополнение к токену Админа, который возвращается методами SignUpAsync и SignInAsync, система дожна обслуживать токены Участника, который возвращается методами SignUpParticipantAsync и SignInParticipantAsync. Токен Участника должен содержать данные Админа (связь по таблице admins_participants).

 Добавь в проект всю необходимую фуекциональность для поддержки как Админов, так и Участников.

 Дай в отдельном файле описание того, что ты сделал и как этим пользоваться.

 Мне нужны тестовые SQL DB данные. Смотри структуру БД в plantour-server\DB\Scripts\plantour.sql. Создай трёх юзеров: одного Админа и двух Участников этого админа. Посмотри в коде plantour-server API как хэшируются и записываются в БД пароли юзеров. Используй Binary_09 пароль для Админа. Для Участников пароль не нужен, но нужен будет AccessCode. Заполни соответоствующие таблицы так, чтобы в них было 10-20 строк. Запиши SQL script в файл plantour-server\DB\Scripts\populate.sql.

 
Создай generic репозиторий GenericRepository<T> в папке Repositories. Создай PackRepository там же. Включи в него CRUD для UserPackage.

Рефактор PackageController. Не вызывай PackRepository из контнроллера. Создай Services/PackService. В сервисе используй PackRepository. Переделай вызовы Add, Edit и другие в PackageController с репозитория на сервис. Используй Riok.Mapperly для маппинга.

Добаавь в PackDto поле CategoryName. Заполняй это поле значением Name из PackageCategory, соответствующим PackDto.CategoryId. Добавь в PackDto список категорий из PackageCategory. Назови его CategoriesLookup. Включи в него Id и Name. Не забудь переделать mappinng соответственно.

Нужно создать сервис, который вернет клиенту полный набор lookups. Создай repository Repositories/LookupsRepository и включи в него только методы для чтения следующих сущностей: PackingStatus, CommunicationType, ThingCategory, TripStatus, ParticipantStatus, Unit. Создай Services/LookupsService и включи в него единственный метод GetAllLookups, который вернет все lookups из Repositories/LookupsRepository  . Создай контролер LookupsController с единственной  endpoint для GetAllLookups.

 и как PasswordHash, PasswordSalt используются в коде. Внеси необходимые изменения в код так, чобы AccessCode использовался также, как сейчас используется Password, т.е. c AccessCodeHash и AccessCodeSalt.





Напиши PostgreSQL stored procedure, которая:
1) на вход получит @adminId uuid, @participantId uuid, @tripId uuiq и массив @ids uuid[]
2) вставит в trip_user_packages записи 

insert into trip_user_packages (trip_user_id, name)
select * 
from 
(
select a.name as name
from user_packages a
where a.id in @ids and a.user_id = @userId

) x
left join 
(
select b.id as trip_user_id, a.name as name
from trip_user_packages a
join trip_users b on b.id = a.trip_user_id
join admins_participants c on c.id = b.admin_participant_id
join trip d on d.id = b.trip_id
where
    d.id = @tripId and
    d.user_id = @aminId and
    c.admin_id = @adminId and
    c.participant_id = @participantId

) y on a.name = b.name
where
    y.trip_user_id is null

Смотри create or replace function plantour.insert_trip_user_packages в файле DB\Scripts\plantour.sql. В repository Repositories\DicTripRepository.cs добавь код для вызова insert_trip_user_packages.


Есть 

public class PackDto
{
    public Guid Id { get; set; }
    public string Name { get; set; } = null!;
    public bool? IsTargeted { get; set; }
}

и 

public class Package
{
    public Guid Id { get; set; }
    public string Name { get; set; } = null!;
}

и

public class TripUserPackage
{
    public Guid Id { get; set; }
    public Guid TripId { get; set; }
    public string Name { get; set; } = null!;
}

Есть коллекции

    IEnumerable<TripUserPackage> tripPackages = await _tripPackageRepository.GetAllAsync();
    IEnumerable<Package> dicPackages = await _userPackageRepository.GetAllAsync();

Есть AutoMapper c помощью которго можно маппить Package в PackDto.

Напиши код, который вернет все записи из dicPackages c маппионгом в PackDto и при этом выставит в true флаг IsTargeted, если в tripPackages найдется запись c таким же Name. Сравнение Names должно работать для любых языков case insensitive.

insert into plantour.trip_shared_things (trip_id, category, name, units, value)
    select
        v_trip_user_id,
        b.category,
        b.name,
        b.units,
        b.value
    from plantour.user_things b
    left join plantour.trip_shared_things c on 
        c.trip_id = p_trip_id and 
        lower(c.name collate "und-x-icu") = lower(b.name collate "und-x-icu")
    where
        b.id = any (p_ids)
        and b.shared 
        and b.user_id = p_admin_id
        and c.id is null


Смотри PostgreSQL скрипт. Это шаблоны для сборов вещей в путешествия. Создай новый PostgreSQL скрипт. Начиная c activites заполни таблицы данными на английском языке. Не нужно включать "Warm sweater" для диапазона температур "Extreme Heat". Данные должны реально помочь путешественникам подобрать набор вещей в дорогу по activity, gender, temperature, age.   Не используй имена полей  таблиц без префикса, т.е. используй "tt.name" вмсето "name". Внимательно следи за тем, чтобы твой код не нарушал constraints, в том числе duplicate keys.  Верни результат в виде, пригодном для копирования c экрана.



-----------------------------------------------------------------------
-- ACTIVITIES
-----------------------------------------------------------------------
create table activities (
    id uuid not null primary key default gen_random_uuid(),
    name text not null unique,
    notes text not null
);

-----------------------------------------------------------------------
-- GENDERS
-----------------------------------------------------------------------
create table genders (
    id uuid not null primary key default gen_random_uuid(),
    name text not null unique,
    notes text null
);
insert into genders (name) values
('Male'),('Female'),('Unisex');

-----------------------------------------------------------------------
-- TEMPERATURE RANGES
-----------------------------------------------------------------------
create table temperature_ranges (
    id uuid not null primary key default gen_random_uuid(),
    name text not null unique,
    fromTemp int null,
    toTemp int null check(toTemp >= fromTemp),
    notes text null
);
insert into temperature_ranges (name, fromTemp, toTemp) values
('Extreme Heat', 31, null),
('Warm', 21, 30),
('Mild', 11, 20),
('Cool', 1, 10),
('Cold', -10, 0),
('Extreme Cold', null, -11);

-----------------------------------------------------------------------
-- AGE RANGES
-----------------------------------------------------------------------
create table age_ranges (
    id uuid not null primary key default gen_random_uuid(),
    name text not null unique,
    fromAge int not null check(fromAge >= 0),
    toAge int null check(toAge >= fromAge),
    notes text null
);
insert into age_ranges (name, fromAge, toAge) values
('Children', 0, 18),
('Early Adulthood', 18, 24),
('Prime Youth', 25, 34),
('Middle Youth', 35, 44),
('Early Seniority', 55, 64),
('Seniority', 65, null);


-----------------------------------------------------------------------
-- THING TEMPLATES
-----------------------------------------------------------------------
create table thing_templates (
    id uuid not null primary key default gen_random_uuid(),
    name text not null unique,
    activity_id uuid not null references activities(id) on delete cascade,
    temperature_ranges_id uuid null references temperature_ranges(id) on delete cascade,
    age_ranges_id uuid null references age_ranges(id) on delete cascade,
    notes text null
);

create table template_things (
    id uuid not null primary key default gen_random_uuid(),
    template_id uuid not null references thing_templates(id) on delete cascade,
    category varchar(50),
    name varchar(200) not null,
    units varchar(50),
    value decimal(10,3) check(value > 0),
    notes text
);
create unique index idx_template_things_template_id_name on template_things(template_id, name);



{Npgsql.PostgresException (0x80004005): 23503: insert or update on table "admins_participants" violates foreign key constraint "admins_participants_participant_status_id_fkey"

DETAIL: Detail redacted as it may contain sensitive data. Specify 'Include Error Detail' in the connection string to include this information.
   at Npgsql.Internal.NpgsqlConnector.ReadMessageLong(Boolean async, DataRowLoadingMode dataRowLoadingMode, Boolean readingNotifications, Boolean isReadingPrependedMessage)
   at System.Runtime.CompilerServices.PoolingAsyncValueTaskMethodBuilder`1.StateMachineBox`1.System.Threading.Tasks.Sources.IValueTaskSource<TResult>.GetResult(Int16 token)
   at Npgsql.NpgsqlDataReader.NextResult(Boolean async, Boolean isConsuming, CancellationToken cancellationToken)
   at Npgsql.NpgsqlDataReader.NextResult(Boolean async, Boolean isConsuming, CancellationToken cancellationToken)
   at Npgsql.NpgsqlCommand.ExecuteReader(Boolean async, CommandBehavior behavior, CancellationToken cancellationToken)
   at Npgsql.NpgsqlCommand.ExecuteReader(Boolean async, CommandBehavior behavior, CancellationToken cancellationToken)
   at Npgsql.NpgsqlCommand.ExecuteDbDataReaderAsync(CommandBehavior behavior, CancellationToken cancellationToken)
   at Microsoft.EntityFrameworkCore.Storage.RelationalCommand.ExecuteReaderAsync(RelationalCommandParameterObject parameterObject, CancellationToken cancellationToken)
   at Microsoft.EntityFrameworkCore.Storage.RelationalCommand.ExecuteReaderAsync(RelationalCommandParameterObject parameterObject, CancellationToken cancellationToken)
   at Microsoft.EntityFrameworkCore.Update.ReaderModificationCommandBatch.ExecuteAsync(IRelationalConnection connection, CancellationToken cancellationToken)
  Exception data:
    Severity: ERROR
    SqlState: 23503
    MessageText: insert or update on table "admins_participants" violates foreign key constraint "admins_participants_participant_status_id_fkey"
    Detail: Detail redacted as it may contain sensitive data. Specify 'Include Error Detail' in the connection string to include this information.
    SchemaName: plantour
    TableName: admins_participants
    ConstraintName: admins_participants_participant_status_id_fkey
    File: ri_triggers.c
    Line: 2783
    Routine: ri_ReportViolation}